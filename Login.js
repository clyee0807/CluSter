import { ImageBackground, StyleSheet, Text, View, TouchableOpacity } from 'react-native';

function Login(){
    return (
        <View>
            <Text>LOGIN</Text>
        </View>
    );
}

export default Login;